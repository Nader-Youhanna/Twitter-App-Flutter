import 'package:flutter/material.dart';

import './tweet.dart';

class Comment extends StatelessWidget {
  final Tweet tweet;
  const Comment(Key? key, this.tweet) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
